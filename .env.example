# Public site URL (used for og:url)
PUBLIC_SITE_URL=https://orelix.be

# Resend API key — get one at https://resend.com (server-only, never commit the real key)
# Local dev: copy to .dev.vars for wrangler preview, or use Cloudflare dashboard in production.
RESEND_API_KEY=
