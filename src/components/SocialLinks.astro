---
import settings from "../content/settings.json";

interface Props {
  variant?: "hero" | "pills";
  label?: string;
}

const { variant = "pills", label } = Astro.props;

const channels = [
  { id: "linkedin", label: "LinkedIn", mark: "in", href: settings.socials.linkedin },
].filter((c) => Boolean(c.href));
---

{channels.length > 0 && (
  <div class={`social-links social-links--${variant}`} aria-label="Sociale media">
    {label && <span class="social-links-label">{label}</span>}
    <div class="social-links-row">
      {channels.map((c) => (
        <a
          href={c.href}
          class="social-pill"
          target="_blank"
          rel="noopener noreferrer"
          data-cursor={c.label}
        >
          <span class="social-icon-mark" aria-hidden="true">{c.mark}</span>
          <span>{c.label}</span>
        </a>
      ))}
    </div>
  </div>
)}
