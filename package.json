{
  "name": "merati-tegelwerken-site",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Merati Tegelwerken - statische website (Astro, gebaseerd op @kashani-studio/template-classic)",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check && astro build",
    "preview": "pnpm run build && wrangler dev",
    "astro": "astro",
    "deploy": "pnpm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.0",
    "@astrojs/cloudflare": "^12.6.13",
    "astro": "^5.0.0",
    "typescript": "^5.6.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  }
}
